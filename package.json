{
  "name": "jev-search",
  "version": "0.1.0",
  "description": "Fast semantic code search: TypeSafe Jev walks the repo folder → file → function.",
  "type": "module",
  "bin": {
    "jev-search": "src/cli.ts"
  },
  "scripts": {
    "check": "tsc"
  },
  "engines": {
    "bun": ">=1.3"
  },
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
